Я помогу вам разобраться, как протестировать это Spring Security приложение с JWT аутентификацией. Сначала мне нужно посмотреть на структуру проекта подробнее.Отлично! Теперь я понимаю структуру приложения. Позвольте я объясню, как протестировать это приложение.

## 📋 Как протестировать приложение Cookie Authentication

Ваше приложение реализует безопасную аутентификацию через защищенные куки с использованием шифрованных JWT-токенов (JWE). Вот пошаговое руководство по тестированию:

---

### 🔧 Подготовка

1. **Запустите PostgreSQL через Docker Compose:**
```shell script
docker compose up -d
```


2. **Запустите приложение cookie-authentication:**
```shell script
mvn spring-boot:run -pl cookie-authentication
```


3. **Откройте браузер по адресу:**
```
https://localhost:8443
```


---

### 🧪 Тестовые данные

По умолчанию в БД создается один пользователь (см. `data.sql`):
- **Логин:** `j.jameson`
- **Пароль:** `password`
- **Роль:** `ROLE_MANAGER`

---

### 🎯 Эндпоинты для тестирования

#### 1. **GET `/csrf`** — Получение CSRF-токена

**Назначение:** Получить CSRF-токен для защиты от Cross-Site Request Forgery атак. Этот токен нужен для выполнения операций, изменяющих состояние (POST, PUT, DELETE).

**Тест через браузер:**
- Откройте `index.html`, нажмите кнопку **"Получить CSRF"**
- В блоке отобразится JSON с токеном

**Тест через curl:**
```shell script
curl -k https://localhost:8443/csrf
```


**Ожидаемый ответ:**
```json
{
  "token": "xxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx",
  "parameterName": "_csrf",
  "headerName": "X-CSRF-TOKEN"
}
```


---

#### 2. **Вход в систему (Basic Authentication + получение CSRF)**

**Способ 1: Через интерфейс на index.html**
1. Введите `j.jameson` в поле "Имя пользователя"
2. Введите `password` в поле "Пароль"
3. Нажмите **"Войти"**
4. Если все успешно, в блоке CSRF появится токен

**Способ 2: Через curl с Basic Auth**
```shell script
curl -k -u j.jameson:password https://localhost:8443/csrf -v
```


**Что происходит:**
- Сервер проверяет учетные данные
- При успехе создается зашифрованный токен `Token`
- Токен записывается в защищенную куку `__Host-auth-token` (HttpOnly, Secure)
- Эта кука автоматически будет присылаться браузером при последующих запросах

---

#### 3. **GET `/api/greetings`** — Защищенный API-эндпоинт

**Назначение:** Возвращает приветствие для аутентифицированного пользователя. Требует наличия валидной куки с токеном.

**Тест через браузер:**
1. Сначала выполните вход (см. шаг 2)
2. Нажмите кнопку **"Привет!"**
3. В блоке отобразится приветствие

**Тест через curl (после входа):**
```shell script
# Сначала сохраните куки при входе
curl -k -u j.jameson:password -c cookies.txt https://localhost:8443/csrf

# Затем используйте сохраненные куки
curl -k -b cookies.txt https://localhost:8443/api/greetings
```


**Ожидаемый ответ:**
```json
{
  "greeting": "Hello, j.jameson!"
}
```


---

#### 4. **POST `/logout`** — Выход из системы

**Назначение:** Деактивирует текущий токен (добавляет его ID в таблицу `t_deactivated_token`) и удаляет куку из браузера.

**Важно:** Для выхода требуется CSRF-токен!

**Тест через браузер:**
1. Получите CSRF-токен (кнопка "Получить CSRF")
2. Нажмите кнопку **"Выйти"**
3. Куки и данные должны очиститься

**Тест через curl:**
```shell script
# 1. Войдите и сохраните куки + получите CSRF
curl -k -u j.jameson:password -c cookies.txt https://localhost:8443/csrf > csrf.json

# 2. Извлеките токен и параметр
CSRF_TOKEN=$(cat csrf.json | grep -o '"token":"[^"]*' | cut -d'"' -f4)
CSRF_PARAM=$(cat csrf.json | grep -o '"parameterName":"[^"]*' | cut -d'"' -f4)

# 3. Выполните выход
curl -k -b cookies.txt -X POST "https://localhost:8443/logout?${CSRF_PARAM}=${CSRF_TOKEN}"
```


---

#### 5. **GET `/manager`** — Страница для роли MANAGER

**Назначение:** Доступ к шаблону страницы менеджера. Требует аутентификации **и** роли `ROLE_MANAGER`.

**Тест:**
```shell script
# С кукой аутентифицированного пользователя
curl -k -b cookies.txt https://localhost:8443/manager
```


**Примечание:** Поскольку у `j.jameson` есть роль `ROLE_MANAGER`, доступ будет разрешен.

---

### 🧪 Сценарии для тестирования

#### ✅ Позитивный сценарий
1. Войдите с валидными учетными данными
2. Получите CSRF-токен
3. Вызовите защищенный эндпоинт `/api/greetings`
4. Получите корректное приветствие
5. Выйдите из системы

#### ❌ Негативные сценарии

**Без аутентификации:**
```shell script
curl -k https://localhost:8443/api/greetings
```

Ожидаемый результат: **401 Unauthorized** или редирект на форму входа

**С неверными учетными данными:**
```shell script
curl -k -u wrong:credentials https://localhost:8443/csrf
```

Ожидаемый результат: **401 Unauthorized**

**Попытка выхода без CSRF-токена:**
```shell script
curl -k -b cookies.txt -X POST https://localhost:8443/logout
```

Ожидаемый результат: **403 Forbidden** (CSRF защита)

**Доступ к `/manager` без роли:**
- Если создать пользователя без `ROLE_MANAGER`, доступ будет запрещен

---

### 🔍 Проверка безопасности

#### Проверьте куку в DevTools браузера
1. Откройте DevTools (F12)
2. Перейдите в Application → Cookies
3. Найдите `__Host-auth-token`
4. Убедитесь в наличии флагов:
    - ✅ HttpOnly (JavaScript не может прочитать)
    - ✅ Secure (передается только по HTTPS)
    - ✅ SameSite=Lax или Strict

#### Попробуйте украсть токен через JavaScript
Откройте консоль браузера и выполните:
```javascript
document.cookie
```

Вы **не увидите** `__Host-auth-token` благодаря флагу `HttpOnly`!

---

### 📊 Проверка БД

После выхода проверьте, что токен был деактивирован:

```sql
SELECT * FROM t_deactivated_token;
```


Вы должны увидеть запись с `id` вашего токена и временем деактивации.

---

### 🎨 Тестирование через UI

Откройте `https://localhost:8443/index.html` и используйте интерфейс:
- Все операции можно выполнить через кнопки
- Результаты отображаются прямо на странице
- Удобно для демонстрации работы приложения

---

Надеюсь, это руководство поможет вам полностью протестировать приложение! Если возникнут вопросы по конкретным эндпоинтам или сценариям — спрашивайте! 🚀