# Руководство по модулю Cookie-аутентификации (для новичков)

Этот модуль реализует современный и безопасный способ аутентификации для Single Page Applications (SPA), таких как React, Vue или Angular. Вместо классических сессий на сервере или небезопасных токенов в `localStorage`, мы используем **шифрованные токены в защищенных куках**.

---

## 🏗 Архитектурный подход: "Stateless Cookies"

Обычно в Spring Security сессия хранится на сервере в памяти (Stateful). Это неудобно при масштабировании (если у вас 10 серверов, им нужно как-то делиться этой памятью). 
В этом проекте мы используем **Stateless** подход (без сохранения состояния), но с сохранением удобства кук:
1. Сервер "забывает" о пользователе сразу после ответа на запрос.
2. Вся информация о пользователе (ID, имя, роли) зашифрована и передана клиенту в куке.
3. При каждом следующем запросе клиент присылает эту куку. Сервер её расшифровывает и мгновенно "вспоминает" пользователя. Это позволяет легко добавлять новые серверы в систему.

---

## 🔒 Технологии безопасности

### 1. JWE (JSON Web Encryption)
В отличие от обычного JWT, который можно прочитать (он просто закодирован в Base64), **JWE** полностью зашифрован. Даже если злоумышленник перехватит токен, он не увидит внутри ни имени пользователя, ни его ролей. Для этого используется библиотека `nimbus-jose-jwt`.

### 2. Защищенные куки (`__Host-` prefix)
Мы сохраняем токен в куку с особыми флагами, которые делают её максимально защищенной:
*   **HttpOnly**: Скрипты на странице (JavaScript) не могут прочитать куку. Это защищает от кражи токена через XSS-атаки (внедрение вредоносного скрипта).
*   **Secure**: Кука передается только через зашифрованное соединение HTTPS.
*   **SameSite=Lax/Strict**: Браузер не отправит куку при запросе со стороннего сайта (защита от CSRF-атак).
*   **__Host- prefix**: Особая метка безопасности, которая гарантирует, что кука привязана только к вашему домену и не может быть подменена с поддоменов.

---

## 🚀 Как работает процесс (Step-by-Step)

### Шаг 1: Вход (Login)
Когда пользователь отправляет логин и пароль:
1. Spring Security проверяет их через `UserDetailsService` (запрос к таблице `t_user`).
2. При успехе срабатывает `TokenCookieSessionAuthenticationStrategy`.
3. Она создает объект `Token` — это "паспорт" сессии, где указано, кто это и когда паспорт "протухнет".
4. `TokenCookieJweStringSerializer` зашифровывает этот токен в длинную строку.
5. Эта строка записывается в куку `__Host-auth-token` и отдается браузеру.

### Шаг 2: Обычный запрос к API (например, список товаров)
Когда браузер запрашивает защищенный ресурс:
1. Он автоматически прикладывает нашу куку к запросу.
2. `TokenCookieAuthenticationConverter` находит куку и достает из нее зашифрованную строку.
3. `TokenCookieJweStringDeserializer` расшифровывает её обратно в объект `Token`.
4. `TokenAuthenticationUserDetailsService` выполняет финальную проверку:
    *   Не истекло ли время действия токена?
    *   Не был ли этот конкретный токен отозван (проверка по "черному списку" в БД)?
5. Если всё в порядке, пользователь считается успешно вошедшим.

### Шаг 3: Выход (Logout)
Когда пользователь нажимает "Выйти":
1. `CookieClearingLogoutHandler` удаляет куку из браузера.
2. Наш кастомный код берет ID этого токена и записывает его в таблицу `t_deactivated_token`.
3. Зачем это нужно? Поскольку наш сервер "без памяти" (Stateless), расшифрованный токен сам по себе выглядит валидным, пока не истекло его время. База деактивированных токенов — это способ мгновенно "аннулировать" паспорт до того, как он истечет официально.

---

## 🛠 Ключевые компоненты (Где искать код)

- **Конфигурация**: `SpaCookieAuthenticationApplication` — сердце проекта, где все детали собираются в единую систему защиты.
- **Шифрование**: `TokenCookieJweStringSerializer` и `Deserializer` — здесь живет логика криптографии.
- **Интеграция**: `TokenCookieAuthenticationConfigurer` — вспомогательный класс, который обучает Spring Security работать с нашими куками.
- **Защита от CSRF**: `GetCsrfTokenFilter` — эндпоинт `/csrf`. Современные SPA должны передавать специальный токен в заголовках для защиты от подделки запросов, и этот фильтр помогает фронтенду его получить.

---

## 🗄 Структура базы данных
- `t_user`: Данные пользователей.
- `t_user_authority`: Роли (права доступа).
- `t_deactivated_token`: Список ID токенов, которые больше не действительны.
